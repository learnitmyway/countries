import { expect, test } from "vitest";
import { Countries } from "./Countries";

test("placeholder to test coverage feature", () => {
  // eslint-disable-next-line no-console
  console.log(Countries);
  expect(true).toBe(true);
});
